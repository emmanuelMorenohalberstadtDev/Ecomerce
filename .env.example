# Copy to .env and fill in. Every variable is required - the app fails fast on missing ones.

# PostgreSQL (compose service + backend datasource)
DB_NAME=ecommerce
DB_USER=ecommerce
DB_PASSWORD=change-me-locally
DB_URL=jdbc:postgresql://localhost:5432/ecommerce
